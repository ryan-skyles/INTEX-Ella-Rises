<!-- 
 Professionalism 4
Ability to add user information and donation 3 -->
<%- include('partials/header') %>

<header class="custom-bg-pink py-5 text-center mb-4">
    <div class="container">
        <h1 class="display-5 fw-bold text-dark">Donation Records</h1>
        <p class="lead text-secondary">Track all contributions and donors.</p>
    </div>
</header>

<main class="container mb-5 flex-grow-1">

    <div class="row mb-4">
        <div class="col-md-6">
            <form action="/admin/donations" method="GET" class="d-flex">
                <input type="text" name="search" class="form-control me-2 rounded-pill" placeholder="Search donors..." value="<%= typeof search != 'undefined' ? search : '' %>">
                <button type="submit" class="btn btn-outline-primary rounded-pill">Search</button>
            </form>
        </div>
    </div>

    <div class="card shadow border-0 rounded-4 overflow-hidden">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="bg-light">
                    <tr>
                        <th class="p-3">Date</th>
                        <th class="p-3">Donor Name</th>
                        <th class="p-3">Email</th>
                        <th class="p-3">Amount</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (donations.length > 0) { %>
                        <% donations.forEach(d => { %>
                        <tr>
                            <td class="p-3"><%= new Date(d.DonationDate).toLocaleDateString() %></td>
                            <td class="p-3 fw-bold"><%= d.ParticipantFirstName %> <%= d.ParticipantLastName %></td>
                            <td class="p-3 text-muted"><%= d.ParticipantEmail %></td>
                            <td class="p-3 fw-bold text-success">$<%= d.DonationAmount %></td>
                        </tr>
                        <% }) %>
                    <% } else { %>
                        <tr><td colspan="4" class="text-center p-4">No donation records found.</td></tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
</main>

<%- include('partials/footer') %>