<%- include('partials/header') %>

<main class="container my-5" style="max-width: 800px;">
    
    <div class="row g-5">
        <!-- 왼쪽: 기부 메시지 -->
        <div class="col-md-5 order-md-last">
            <h4 class="d-flex justify-content-between align-items-center mb-3">
                <span class="text-primary">Your Impact</span>
            </h4>
            <ul class="list-group mb-3 shadow-sm rounded-4">
                <li class="list-group-item d-flex justify-content-between lh-sm p-3">
                    <div>
                        <h6 class="my-0 fw-bold">Support a Student</h6>
                        <small class="text-muted">Provides materials for one workshop</small>
                    </div>
                    <span class="text-muted">$25</span>
                </li>
                <li class="list-group-item d-flex justify-content-between lh-sm p-3">
                    <div>
                        <h6 class="my-0 fw-bold">Sponsor a Mentor</h6>
                        <small class="text-muted">Funds a mentoring session</small>
                    </div>
                    <span class="text-muted">$50</span>
                </li>
                <li class="list-group-item d-flex justify-content-between lh-sm p-3">
                    <div>
                        <h6 class="my-0 fw-bold">Technology Fund</h6>
                        <small class="text-muted">Ipads and software licenses</small>
                    </div>
                    <span class="text-muted">$100</span>
                </li>
                <li class="list-group-item d-flex justify-content-between bg-light p-3">
                    <div class="text-success">
                        <h6 class="my-0">Total Goal</h6>
                        <small>Help us reach $50,000</small>
                    </div>
                </li>
            </ul>
        </div>

        <!-- 오른쪽: 기부 폼 -->
        <div class="col-md-7">
            <h4 class="mb-3 fw-bold">Make a Donation</h4>
            <form action="/donate" method="POST" class="needs-validation">
                
                <!-- 1. 금액 선택 -->
                <h6 class="mb-3">Select Amount</h6>
                <div class="btn-group w-100 mb-3" role="group">
                    <input type="radio" class="btn-check" name="amountBtn" id="btn25" autocomplete="off" onclick="document.getElementById('customAmount').value=25">
                    <label class="btn btn-outline-primary" for="btn25">$25</label>

                    <input type="radio" class="btn-check" name="amountBtn" id="btn50" autocomplete="off" onclick="document.getElementById('customAmount').value=50">
                    <label class="btn btn-outline-primary" for="btn50">$50</label>

                    <input type="radio" class="btn-check" name="amountBtn" id="btn100" autocomplete="off" onclick="document.getElementById('customAmount').value=100">
                    <label class="btn btn-outline-primary" for="btn100">$100</label>
                </div>
                
                <div class="input-group mb-4">
                    <span class="input-group-text">$</span>
                    <input type="number" name="amount" id="customAmount" class="form-control" placeholder="Enter custom amount" required min="1">
                </div>

                <hr class="my-4">

                <!-- 2. 기부자 정보 -->
                <h6 class="mb-3">Donor Information</h6>
                <div class="row g-3">
                    <div class="col-sm-6">
                        <label class="form-label small text-muted fw-bold">First name</label>
                        <!-- 로그인했으면 자동 입력 -->
                        <input type="text" name="firstName" class="form-control rounded-pill" required 
                               value="<%= (user && user.firstName) ? user.firstName : '' %>">
                    </div>

                    <div class="col-sm-6">
                        <label class="form-label small text-muted fw-bold">Last name</label>
                        <input type="text" name="lastName" class="form-control rounded-pill" required
                               value="<%= (user && user.lastName) ? user.lastName : '' %>">
                    </div>

                    <div class="col-12">
                        <label class="form-label small text-muted fw-bold">Email</label>
                        <input type="email" name="email" class="form-control rounded-pill" placeholder="you@example.com" required
                               value="<%= (user && user.id) ? user.id : '' %>">
                    </div>
                </div>

                <hr class="my-4">

                <!-- 3. 결제 (Mockup) -->
                <h6 class="mb-3">Payment Details <span class="text-muted small">(Secure)</span></h6>
                <div class="row gy-3">
                    <div class="col-md-6">
                        <label class="form-label small text-muted fw-bold">Name on card</label>
                        <input type="text" class="form-control rounded-pill" required>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label small text-muted fw-bold">Credit card number</label>
                        <input type="text" class="form-control rounded-pill" required>
                    </div>
                    <div class="col-md-3">
                        <label class="form-label small text-muted fw-bold">Expiration</label>
                        <input type="text" class="form-control rounded-pill" placeholder="MM/YY" required>
                    </div>
                    <div class="col-md-3">
                        <label class="form-label small text-muted fw-bold">CVV</label>
                        <input type="text" class="form-control rounded-pill" required>
                    </div>
                </div>

                <hr class="my-4">

                <button class="w-100 btn btn-custom-pink btn-lg rounded-pill fw-bold shadow-sm" type="submit">
                    Complete Donation
                </button>
            </form>
        </div>
    </div>
</main>

<%- include('partials/footer') %>